name: Update README

on:
  schedule:
    - cron: '*/30 * * * *'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    name: Update this repo's README with recent activity

    steps:
      - uses: hadron404/social_readme@main
        env:
          GITHUB_TOKEN: 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
